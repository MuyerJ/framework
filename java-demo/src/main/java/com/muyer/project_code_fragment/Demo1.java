package com.muyer.project_code_fragment;

/**
 * Description:
 * date: 2020/12/26 18:42
 *
 * @author YeJiang
 */
public class Demo1 {
    /**
     * 对比审核日志两个记录是不是一样
     */
    private static void fun1() {
        //for (int i = 0; i < as.size(); i++) {
        //    PriceExtJsonDTO asDTO = as.get(i);
        //    //判断数据是否修改过
        //    // 只要有一个一样==> 设置为false，代表未修改过
        //    asDTO.setModify(bs.stream().noneMatch(b -> ProductExtPriceHelper.isSame(asDTO, b)));
        //}

    }
}
